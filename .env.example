# Backend API base URL (no trailing slash, do NOT include /api – we call /api/category, /api/product/search, etc.)
# When set: categories, products, product detail, and checkout use your backend instead of mock data.
VITE_API_BASE_URL=http://localhost:9090

